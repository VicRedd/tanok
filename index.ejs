 <% include('layouts/layout') %>
<section>
    <h2>Sobre Nosotros</h2>
    <p>Bienvenidos a TANOK. Esta es una empresa dedicada a...</p>
</section>
<section>
    <h2>Proyectos</h2>
    <ul>
        <% projects.forEach(project => { %>
            <li>
                <h3><%= project.name %></h3>
                <p><%= project.description %></p>
            </li>
        <% }) %>
    </ul>
</section>

